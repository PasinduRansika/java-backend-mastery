In Task 1, I implemented a LinkedList for the StudentService because adding to the front of an ArrayList requires O(n) time due to element shifting, while LinkedList handles it in O(1) time.

Smart Hub - I developed a multi-threaded Smart Hub inventory system in Java that ensures thread-safe stock management using ConcurrentHashMap and AtomicInteger. It features a custom persistence layer for saving and loading data via CSV files, alongside a Java Stream-based analytics module for real-time low stock alerts.

GymBuddy — I engineered a robust Java-based reservation system that orchestrates gym operations through a centralized Service Layer and defensive validation engine. The system enforces critical business rules, including a 24-hour cancellation policy and weekly enrollment limits using custom exceptions and Java Streams to ensure data integrity and a seamless user experience.
